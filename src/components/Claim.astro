---
import { renderMarkdown } from '@/utils/markdown'

type Props = {
  claim?: string;
};

const { claim } = Astro.props;
---

{
  claim && (
    <div class="max-w-[46rem] mx-auto my-12 px-4 sm:px-8 text-2xl italic [&_a]:underline" set:html={renderMarkdown(claim)} />
  )
}
